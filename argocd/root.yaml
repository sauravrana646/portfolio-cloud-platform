# Deprecated monolithic root (deployed all envs).
# Use instead:
#   kubectl apply -f argocd/root-bootstrap.yaml   # Kyverno, monitoring, …
#   kubectl apply -f argocd/root-dev.yaml         # workloads with apps/dev.yaml only
#   kubectl apply -f argocd/root-uat.yaml
#   kubectl apply -f argocd/root-prod.yaml

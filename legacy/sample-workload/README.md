# Legacy sample workload (not used)

This folder keeps the old in-repo Flask API + Redis worker that previously
demoed Compose/Helm before the platform pack switched to the signed image from
[`portfolio-secure-cicd`](https://github.com/sauravrana646/portfolio-secure-cicd).

**Do not deploy these images in the default path.** Runtime uses:

`ghcr.io/sauravrana646/portfolio-secure-cicd@sha256:…`

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
  # Upstream portfolio-secure-cicd currently exposes /healthz, not /metrics.
  # Keep Prometheus for the local observability story; add blackbox or upstream
  # metrics when available.
